# Required: keep these in .env only (never commit real values)
# All other settings live in data/settings.json and can be updated in chat (saved to data/config_overrides.json).

# Public.com API (required)
PUBLIC_SECRET_KEY=your_api_secret_key_here

# Optional: for headless/deploy; otherwise interactive or data/bot_config.json
# PUBLIC_ACCOUNT_NUMBER=your_account_id_here

# Telegram bot (required for Telegram bot)
TELEGRAM_BOT_TOKEN=
OPENAI_API_KEY=

# Optional: comma-separated Telegram user IDs allowed to execute trades / change config (empty = read-only for all)
# ALLOWED_TELEGRAM_USER_IDS=
